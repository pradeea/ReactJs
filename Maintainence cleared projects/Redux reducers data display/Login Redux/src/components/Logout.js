import React from 'react'

const Logout = () => {
    return (
        <div>
            <h1>Welcome<span className="">Pradeep</span></h1>
            <button type="button" className="btn btn-danger">Logout</button>
        </div>
    )
}

export default Logout
